<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Contact Us Details</title>
    <style>
      body { font-family: 'Arial', sans-serif; line-height: 1.6; color: #333; background-color:
      #f9f9f9; margin: 0; padding: 0; } .email-container { max-width: 600px; margin: 30px auto;
      background: #ffffff; border-radius: 10px; overflow: hidden; box-shadow: 0 4px 10px rgba(0, 0,
      0, 0.1); } .header { background: linear-gradient(135deg, #4e73df, #2c9cdb); color: #ffffff;
      padding: 20px; text-align: center; font-size: 24px; font-weight: bold; text-transform:
      uppercase; letter-spacing: 1px; } .content { padding: 30px; } table { width: 100%;
      border-collapse: collapse; margin-bottom: 20px; } th, td { padding: 15px; text-align: left;
      border-bottom: 1px solid #e6e6e6; } th { background-color: #f3f5f7; font-weight: bold;
      text-transform: uppercase; font-size: 14px; } td { font-size: 14px; color: #555; } .message {
      padding: 20px; background: #f9f9f9; border-left: 5px solid #2c9cdb; border-radius: 5px;
      font-size: 14px; font-style: italic; color: #555; } .footer { background: #f3f5f7; text-align:
      center; padding: 15px; font-size: 12px; color: #888; }
    </style>
  </head>
  <body>
    <div class='email-container'>
      <div class='header'>
        Contact Us Details
      </div>
      <div class='content'>
        <table>
          {{#each contactDetails}}
            <tr>
              <th>{{label}}</th>
              <td>{{content}}</td>
            </tr>
          {{/each}}
        </table>
        <div class='message'>
          <strong>Message:</strong>
          {{message}}
        </div>
      </div>
      <div class='footer'>
        This is an automated email.
      </div>
    </div>
  </body>
</html>